import { SpatialDemurrageMap } from './spatial-demurrage-map';

/**
 * All components available in MDX content.
 * Add new components here to make them available in notes.
 */
export const mdxComponents = {
  // Maps
  SpatialDemurrageMap,

  // Future components can be added here:
  // DataChart,
  // InteractiveTimeline,
  // etc.
};

export { SpatialDemurrageMap };
